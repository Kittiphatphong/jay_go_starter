app:
  port: 9000

postgres:
  host: localhost
  port: 5432
  user postgres
  password postgres
  database postgres 

redis:
  host: localhost
  port: 6379

auth:
  secretWeb: your-web-jwt-secret
  secretApi: your-api-jwt-secret